import { z } from "zod";
import { workflowEvent } from "@llamaindex/workflow-core";

//#region src/util/zod.ts
const zodEvent = (schema, config) => {
	const event = workflowEvent(config);
	event.onInit(({ data }) => {
		schema.parse(data);
	});
	return Object.assign(event, { get schema() {
		return schema;
	} });
};

//#endregion
export { zodEvent };
//# sourceMappingURL=zod.js.map