Object.defineProperty(exports, '__esModule', { value: true });

var core = require('@llama-flow/core');
var state = require('@llama-flow/core/middleware/state');
var traceEvents = require('@llama-flow/core/middleware/trace-events');
var pRetry = require('@llama-flow/core/util/p-retry');
var env = require('@llamaindex/env');
var zod$1 = require('@llama-flow/core/util/zod');
var zod = require('zod');

var A = async (t, r)=>{
    let e = typeof r == "function" ? await r(t) : r;
    if (e) return t.scheme === "bearer" ? `Bearer ${e}` : t.scheme === "basic" ? `Basic ${btoa(e)}` : e;
}, z = (t, r, e)=>{
    typeof e == "string" || e instanceof Blob ? t.append(r, e) : t.append(r, JSON.stringify(e));
}, k = {
    bodySerializer: (t)=>{
        let r = new FormData;
        return Object.entries(t).forEach(([e, a])=>{
            a != null && (Array.isArray(a) ? a.forEach((i)=>z(r, e, i)) : z(r, e, a));
        }), r;
    }
}, R = {
    bodySerializer: (t)=>JSON.stringify(t, (r, e)=>typeof e == "bigint" ? e.toString() : e)
}, U = (t)=>{
    switch(t){
        case "label":
            return ".";
        case "matrix":
            return ";";
        case "simple":
            return ",";
        default:
            return "&";
    }
}, _ = (t)=>{
    switch(t){
        case "form":
            return ",";
        case "pipeDelimited":
            return "|";
        case "spaceDelimited":
            return "%20";
        default:
            return ",";
    }
}, D = (t)=>{
    switch(t){
        case "label":
            return ".";
        case "matrix":
            return ";";
        case "simple":
            return ",";
        default:
            return "&";
    }
}, O = ({ allowReserved: t, explode: r, name: e, style: a, value: i })=>{
    if (!r) {
        let s = (t ? i : i.map((l)=>encodeURIComponent(l))).join(_(a));
        switch(a){
            case "label":
                return `.${s}`;
            case "matrix":
                return `;${e}=${s}`;
            case "simple":
                return s;
            default:
                return `${e}=${s}`;
        }
    }
    let o = U(a), n = i.map((s)=>a === "label" || a === "simple" ? t ? s : encodeURIComponent(s) : y({
            allowReserved: t,
            name: e,
            value: s
        })).join(o);
    return a === "label" || a === "matrix" ? o + n : n;
}, y = ({ allowReserved: t, name: r, value: e })=>{
    if (e == null) return "";
    if (typeof e == "object") throw new Error("Deeply-nested arrays/objects aren\u2019t supported. Provide your own `querySerializer()` to handle these.");
    return `${r}=${t ? e : encodeURIComponent(e)}`;
}, q = ({ allowReserved: t, explode: r, name: e, style: a, value: i })=>{
    if (i instanceof Date) return `${e}=${i.toISOString()}`;
    if (a !== "deepObject" && !r) {
        let s = [];
        Object.entries(i).forEach(([f, u])=>{
            s = [
                ...s,
                f,
                t ? u : encodeURIComponent(u)
            ];
        });
        let l = s.join(",");
        switch(a){
            case "form":
                return `${e}=${l}`;
            case "label":
                return `.${l}`;
            case "matrix":
                return `;${e}=${l}`;
            default:
                return l;
        }
    }
    let o = D(a), n = Object.entries(i).map(([s, l])=>y({
            allowReserved: t,
            name: a === "deepObject" ? `${e}[${s}]` : s,
            value: l
        })).join(o);
    return a === "label" || a === "matrix" ? o + n : n;
};
var H = /\{[^{}]+\}/g, B = ({ path: t, url: r })=>{
    let e = r, a = r.match(H);
    if (a) for (let i of a){
        let o = false, n = i.substring(1, i.length - 1), s = "simple";
        n.endsWith("*") && (o = true, n = n.substring(0, n.length - 1)), n.startsWith(".") ? (n = n.substring(1), s = "label") : n.startsWith(";") && (n = n.substring(1), s = "matrix");
        let l = t[n];
        if (l == null) continue;
        if (Array.isArray(l)) {
            e = e.replace(i, O({
                explode: o,
                name: n,
                style: s,
                value: l
            }));
            continue;
        }
        if (typeof l == "object") {
            e = e.replace(i, q({
                explode: o,
                name: n,
                style: s,
                value: l
            }));
            continue;
        }
        if (s === "matrix") {
            e = e.replace(i, `;${y({
                name: n,
                value: l
            })}`);
            continue;
        }
        let f = encodeURIComponent(s === "label" ? `.${l}` : l);
        e = e.replace(i, f);
    }
    return e;
}, P = ({ allowReserved: t, array: r, object: e } = {})=>(i)=>{
        let o = [];
        if (i && typeof i == "object") for(let n in i){
            let s = i[n];
            if (s != null) if (Array.isArray(s)) {
                let l = O({
                    allowReserved: t,
                    explode: true,
                    name: n,
                    style: "form",
                    value: s,
                    ...r
                });
                l && o.push(l);
            } else if (typeof s == "object") {
                let l = q({
                    allowReserved: t,
                    explode: true,
                    name: n,
                    style: "deepObject",
                    value: s,
                    ...e
                });
                l && o.push(l);
            } else {
                let l = y({
                    allowReserved: t,
                    name: n,
                    value: s
                });
                l && o.push(l);
            }
        }
        return o.join("&");
    }, E = (t)=>{
    if (!t) return "stream";
    let r = t.split(";")[0]?.trim();
    if (r) {
        if (r.startsWith("application/json") || r.endsWith("+json")) return "json";
        if (r === "multipart/form-data") return "formData";
        if ([
            "application/",
            "audio/",
            "image/",
            "video/"
        ].some((e)=>r.startsWith(e))) return "blob";
        if (r.startsWith("text/")) return "text";
    }
}, I = async ({ security: t, ...r })=>{
    for (let e of t){
        let a = await A(e, r.auth);
        if (!a) continue;
        let i = e.name ?? "Authorization";
        switch(e.in){
            case "query":
                r.query || (r.query = {}), r.query[i] = a;
                break;
            case "cookie":
                r.headers.append("Cookie", `${i}=${a}`);
                break;
            case "header":
            default:
                r.headers.set(i, a);
                break;
        }
        return;
    }
}, S = (t)=>W({
        baseUrl: t.baseUrl,
        path: t.path,
        query: t.query,
        querySerializer: typeof t.querySerializer == "function" ? t.querySerializer : P(t.querySerializer),
        url: t.url
    }), W = ({ baseUrl: t, path: r, query: e, querySerializer: a, url: i })=>{
    let o = i.startsWith("/") ? i : `/${i}`, n = (t ?? "") + o;
    r && (n = B({
        path: r,
        url: n
    }));
    let s = e ? a(e) : "";
    return s.startsWith("?") && (s = s.substring(1)), s && (n += `?${s}`), n;
}, C = (t, r)=>{
    let e = {
        ...t,
        ...r
    };
    return e.baseUrl?.endsWith("/") && (e.baseUrl = e.baseUrl.substring(0, e.baseUrl.length - 1)), e.headers = x(t.headers, r.headers), e;
}, x = (...t)=>{
    let r = new Headers;
    for (let e of t){
        if (!e || typeof e != "object") continue;
        let a = e instanceof Headers ? e.entries() : Object.entries(e);
        for (let [i, o] of a)if (o === null) r.delete(i);
        else if (Array.isArray(o)) for (let n of o)r.append(i, n);
        else o !== void 0 && r.set(i, typeof o == "object" ? JSON.stringify(o) : o);
    }
    return r;
}, h = class {
    constructor(){
        this._fns = [];
    }
    clear() {
        this._fns = [];
    }
    exists(r) {
        return this._fns.indexOf(r) !== -1;
    }
    eject(r) {
        let e = this._fns.indexOf(r);
        e !== -1 && (this._fns = [
            ...this._fns.slice(0, e),
            ...this._fns.slice(e + 1)
        ]);
    }
    use(r) {
        this._fns = [
            ...this._fns,
            r
        ];
    }
}, v = ()=>({
        error: new h,
        request: new h,
        response: new h
    }), N = P({
    allowReserved: false,
    array: {
        explode: true,
        style: "form"
    },
    object: {
        explode: true,
        style: "deepObject"
    }
}), Q = {
    "Content-Type": "application/json"
}, w = (t = {})=>({
        ...R,
        headers: Q,
        parseAs: "auto",
        querySerializer: N,
        ...t
    });
var J = (t = {})=>{
    let r = C(w(), t), e = ()=>({
            ...r
        }), a = (n)=>(r = C(r, n), e()), i = v(), o = async (n)=>{
        let s = {
            ...r,
            ...n,
            fetch: n.fetch ?? r.fetch ?? globalThis.fetch,
            headers: x(r.headers, n.headers)
        };
        s.security && await I({
            ...s,
            security: s.security
        }), s.body && s.bodySerializer && (s.body = s.bodySerializer(s.body)), (s.body === void 0 || s.body === "") && s.headers.delete("Content-Type");
        let l = S(s), f = {
            redirect: "follow",
            ...s
        }, u = new Request(l, f);
        for (let p of i.request._fns)u = await p(u, s);
        let T = s.fetch, c = await T(u);
        for (let p of i.response._fns)c = await p(c, u, s);
        let m = {
            request: u,
            response: c
        };
        if (c.ok) {
            if (c.status === 204 || c.headers.get("Content-Length") === "0") return {
                data: {},
                ...m
            };
            let p = (s.parseAs === "auto" ? E(c.headers.get("Content-Type")) : s.parseAs) ?? "json";
            if (p === "stream") return {
                data: c.body,
                ...m
            };
            let b = await c[p]();
            return p === "json" && (s.responseValidator && await s.responseValidator(b), s.responseTransformer && (b = await s.responseTransformer(b))), {
                data: b,
                ...m
            };
        }
        let g = await c.text();
        try {
            g = JSON.parse(g);
        } catch  {}
        let d = g;
        for (let p of i.error._fns)d = await p(g, c, u, s);
        if (d = d || {}, s.throwOnError) throw d;
        return {
            error: d,
            ...m
        };
    };
    return {
        buildUrl: S,
        connect: (n)=>o({
                ...n,
                method: "CONNECT"
            }),
        delete: (n)=>o({
                ...n,
                method: "DELETE"
            }),
        get: (n)=>o({
                ...n,
                method: "GET"
            }),
        getConfig: e,
        head: (n)=>o({
                ...n,
                method: "HEAD"
            }),
        interceptors: i,
        options: (n)=>o({
                ...n,
                method: "OPTIONS"
            }),
        patch: (n)=>o({
                ...n,
                method: "PATCH"
            }),
        post: (n)=>o({
                ...n,
                method: "POST"
            }),
        put: (n)=>o({
                ...n,
                method: "PUT"
            }),
        request: o,
        setConfig: a,
        trace: (n)=>o({
                ...n,
                method: "TRACE"
            })
    };
};

// This file is auto-generated by @hey-api/openapi-ts
const client = J(w());

// This file is auto-generated by @hey-api/openapi-ts
/**
 * Upload File
 */ const uploadFileApiV1ParsingUploadPost = (options)=>{
    return (options?.client ?? client).post({
        ...k,
        security: [
            {
                scheme: "bearer",
                type: "http"
            },
            {
                scheme: "bearer",
                type: "http"
            }
        ],
        url: "/api/v1/parsing/upload",
        ...options,
        headers: {
            "Content-Type": null,
            ...options?.headers
        }
    });
};
/**
 * Get Job
 * Get a job by id
 */ const getJobApiV1ParsingJobJobIdGet = (options)=>{
    return (options.client ?? client).get({
        security: [
            {
                scheme: "bearer",
                type: "http"
            },
            {
                scheme: "bearer",
                type: "http"
            }
        ],
        url: "/api/v1/parsing/job/{job_id}",
        ...options
    });
};
/**
 * Get Job Text Result
 * Get a job by id
 *
 * Note: The 'credits_used' and 'job_credits_usage' fields in the response metadata are deprecated
 * and will be removed in a future release.
 */ const getJobTextResultApiV1ParsingJobJobIdResultTextGet = (options)=>{
    return (options.client ?? client).get({
        security: [
            {
                scheme: "bearer",
                type: "http"
            },
            {
                scheme: "bearer",
                type: "http"
            }
        ],
        url: "/api/v1/parsing/job/{job_id}/result/text",
        ...options
    });
};
/**
 * Get Job Result
 * Get a job by id
 *
 * Note: The 'credits_used' and 'job_credits_usage' fields in the response metadata are deprecated
 * and will be removed in a future release.
 */ const getJobResultApiV1ParsingJobJobIdResultMarkdownGet = (options)=>{
    return (options.client ?? client).get({
        security: [
            {
                scheme: "bearer",
                type: "http"
            },
            {
                scheme: "bearer",
                type: "http"
            }
        ],
        url: "/api/v1/parsing/job/{job_id}/result/markdown",
        ...options
    });
};
/**
 * Get Job Json Result
 * Get a job by id
 *
 * Note: The 'credits_used' and 'job_credits_usage' fields in the response metadata are deprecated
 * and will be removed in a future release.
 */ const getJobJsonResultApiV1ParsingJobJobIdResultJsonGet = (options)=>{
    return (options.client ?? client).get({
        security: [
            {
                scheme: "bearer",
                type: "http"
            },
            {
                scheme: "bearer",
                type: "http"
            }
        ],
        url: "/api/v1/parsing/job/{job_id}/result/json",
        ...options
    });
};

// This file is auto-generated by @hey-api/openapi-ts
/**
 * Enum for representing the different available page error handling modes
 */ const FailPageMode = {
    RAW_TEXT: "raw_text",
    BLANK_PAGE: "blank_page",
    ERROR_MESSAGE: "error_message"
};
/**
 * Enum for representing the languages supported by the parser
 */ const ParserLanguages = {
    AF: "af",
    AZ: "az",
    BS: "bs",
    CS: "cs",
    CY: "cy",
    DA: "da",
    DE: "de",
    EN: "en",
    ES: "es",
    ET: "et",
    FR: "fr",
    GA: "ga",
    HR: "hr",
    HU: "hu",
    ID: "id",
    IS: "is",
    IT: "it",
    KU: "ku",
    LA: "la",
    LT: "lt",
    LV: "lv",
    MI: "mi",
    MS: "ms",
    MT: "mt",
    NL: "nl",
    NO: "no",
    OC: "oc",
    PI: "pi",
    PL: "pl",
    PT: "pt",
    RO: "ro",
    RS_LATIN: "rs_latin",
    SK: "sk",
    SL: "sl",
    SQ: "sq",
    SV: "sv",
    SW: "sw",
    TL: "tl",
    TR: "tr",
    UZ: "uz",
    VI: "vi",
    AR: "ar",
    FA: "fa",
    UG: "ug",
    UR: "ur",
    BN: "bn",
    AS: "as",
    MNI: "mni",
    RU: "ru",
    RS_CYRILLIC: "rs_cyrillic",
    BE: "be",
    BG: "bg",
    UK: "uk",
    MN: "mn",
    ABQ: "abq",
    ADY: "ady",
    KBD: "kbd",
    AVA: "ava",
    DAR: "dar",
    INH: "inh",
    CHE: "che",
    LBE: "lbe",
    LEZ: "lez",
    TAB: "tab",
    TJK: "tjk",
    HI: "hi",
    MR: "mr",
    NE: "ne",
    BH: "bh",
    MAI: "mai",
    ANG: "ang",
    BHO: "bho",
    MAH: "mah",
    SCK: "sck",
    NEW: "new",
    GOM: "gom",
    SA: "sa",
    BGC: "bgc",
    TH: "th",
    CH_SIM: "ch_sim",
    CH_TRA: "ch_tra",
    JA: "ja",
    KO: "ko",
    TA: "ta",
    TE: "te",
    KN: "kn"
};
/**
 * Enum for representing the mode of parsing to be used
 */ const ParsingMode = {
    PARSE_PAGE_WITHOUT_LLM: "parse_page_without_llm",
    PARSE_PAGE_WITH_LLM: "parse_page_with_llm",
    PARSE_PAGE_WITH_LVM: "parse_page_with_lvm",
    PARSE_PAGE_WITH_AGENT: "parse_page_with_agent",
    PARSE_PAGE_WITH_LAYOUT_AGENT: "parse_page_with_layout_agent",
    PARSE_DOCUMENT_WITH_LLM: "parse_document_with_llm",
    PARSE_DOCUMENT_WITH_LVM: "parse_document_with_lvm",
    PARSE_DOCUMENT_WITH_AGENT: "parse_document_with_agent"
};

const VALUES = [
    ParserLanguages.EN,
    ...Object.values(ParserLanguages)
];
const languageSchema = zod.z.enum(VALUES);
const PARSE_PRESETS = [
    "fast",
    "balanced",
    "premium",
    "structured",
    "auto",
    "scientific",
    "invoice",
    "slides",
    "_carlyle"
];
const parsePresetSchema = zod.z.enum(PARSE_PRESETS);
const parseFormSchema = zod.z.object({
    adaptive_long_table: zod.z.boolean().optional(),
    annotate_links: zod.z.boolean().optional(),
    auto_mode: zod.z.boolean().optional(),
    auto_mode_trigger_on_image_in_page: zod.z.boolean().optional(),
    auto_mode_trigger_on_table_in_page: zod.z.boolean().optional(),
    auto_mode_trigger_on_text_in_page: zod.z.string().optional(),
    auto_mode_trigger_on_regexp_in_page: zod.z.string().optional(),
    auto_mode_configuration_json: zod.z.string().optional(),
    azure_openai_api_version: zod.z.string().optional(),
    azure_openai_deployment_name: zod.z.string().optional(),
    azure_openai_endpoint: zod.z.string().optional(),
    azure_openai_key: zod.z.string().optional(),
    bbox_bottom: zod.z.number().min(0).max(1).optional(),
    bbox_left: zod.z.number().min(0).max(1).optional(),
    bbox_right: zod.z.number().min(0).max(1).optional(),
    bbox_top: zod.z.number().min(0).max(1).optional(),
    disable_ocr: zod.z.boolean().optional(),
    disable_reconstruction: zod.z.boolean().optional(),
    disable_image_extraction: zod.z.boolean().optional(),
    do_not_cache: zod.z.coerce.boolean().optional(),
    do_not_unroll_columns: zod.z.coerce.boolean().optional(),
    extract_charts: zod.z.boolean().optional(),
    guess_xlsx_sheet_name: zod.z.boolean().optional(),
    html_make_all_elements_visible: zod.z.boolean().optional(),
    html_remove_fixed_elements: zod.z.boolean().optional(),
    html_remove_navigation_elements: zod.z.boolean().optional(),
    http_proxy: zod.z.string().url('Set a valid URL for the HTTP proxy, e.g., "http://proxy.example.com:8080"').refine((url)=>{
        try {
            const parsedUrl = new URL(url);
            return parsedUrl.protocol === "http:" || parsedUrl.protocol === "https:";
        } catch  {
            return false;
        }
    }, {
        message: "Invalid HTTP proxy URL"
    }).optional(),
    input_s3_path: zod.z.string().optional(),
    input_s3_region: zod.z.string().optional(),
    input_url: zod.z.string().optional(),
    invalidate_cache: zod.z.boolean().optional(),
    language: zod.z.array(languageSchema).optional(),
    extract_layout: zod.z.boolean().optional(),
    max_pages: zod.z.number().nullable().optional(),
    output_pdf_of_document: zod.z.boolean().optional(),
    output_s3_path_prefix: zod.z.string().optional(),
    output_s3_region: zod.z.string().optional(),
    page_prefix: zod.z.string().optional(),
    page_separator: zod.z.string().optional(),
    page_suffix: zod.z.string().optional(),
    preserve_layout_alignment_across_pages: zod.z.boolean().optional(),
    skip_diagonal_text: zod.z.boolean().optional(),
    spreadsheet_extract_sub_tables: zod.z.boolean().optional(),
    structured_output: zod.z.boolean().optional(),
    structured_output_json_schema: zod.z.string().optional(),
    structured_output_json_schema_name: zod.z.string().optional(),
    take_screenshot: zod.z.boolean().optional(),
    target_pages: zod.z.string().optional(),
    vendor_multimodal_api_key: zod.z.string().optional(),
    vendor_multimodal_model_name: zod.z.string().optional(),
    model: zod.z.string().optional(),
    webhook_url: zod.z.string().url().optional(),
    parse_mode: zod.z.nativeEnum(ParsingMode).nullable().optional(),
    system_prompt: zod.z.string().optional(),
    system_prompt_append: zod.z.string().optional(),
    user_prompt: zod.z.string().optional(),
    job_timeout_in_seconds: zod.z.number().optional(),
    job_timeout_extra_time_per_page_in_seconds: zod.z.number().optional(),
    strict_mode_image_extraction: zod.z.boolean().optional(),
    strict_mode_image_ocr: zod.z.boolean().optional(),
    strict_mode_reconstruction: zod.z.boolean().optional(),
    strict_mode_buggy_font: zod.z.boolean().optional(),
    save_images: zod.z.boolean().optional(),
    ignore_document_elements_for_layout_detection: zod.z.boolean().optional(),
    output_tables_as_HTML: zod.z.boolean().optional(),
    use_vendor_multimodal_model: zod.z.boolean().optional(),
    bounding_box: zod.z.string().optional(),
    gpt4o_mode: zod.z.boolean().optional(),
    gpt4o_api_key: zod.z.string().optional(),
    complemental_formatting_instruction: zod.z.string().optional(),
    content_guideline_instruction: zod.z.string().optional(),
    premium_mode: zod.z.boolean().optional(),
    is_formatting_instruction: zod.z.boolean().optional(),
    continuous_mode: zod.z.boolean().optional(),
    parsing_instruction: zod.z.string().optional(),
    fast_mode: zod.z.boolean().optional(),
    formatting_instruction: zod.z.string().optional(),
    preset: parsePresetSchema.optional(),
    compact_markdown_table: zod.z.boolean().optional(),
    markdown_table_multiline_header_separator: zod.z.string().optional(),
    page_error_tolerance: zod.z.number().min(0).max(1).optional(),
    replace_failed_page_mode: zod.z.nativeEnum(FailPageMode).nullable().optional(),
    replace_failed_page_with_error_message_prefix: zod.z.string().optional(),
    replace_failed_page_with_error_message_suffix: zod.z.string().optional()
});

const uploadEvent = zod$1.zodEvent(parseFormSchema.merge(zod.z.object({
    file: zod.z.string().or(zod.z.instanceof(File)).or(zod.z.instanceof(Blob)).or(zod.z.instanceof(Uint8Array)).optional().describe("input")
})), {
    debugLabel: "upload",
    uniqueId: "52099967-34a8-419b-8950-c859eab60145"
});
const checkStatusEvent = core.workflowEvent({
    debugLabel: "check-status",
    uniqueId: "462157fc-1ded-4ba7-9bc4-3e870395bd20"
});
const checkStatusSuccessEvent = core.workflowEvent({
    debugLabel: "check-status-success",
    uniqueId: "360b7641-30f7-456e-851d-104bb5e3f8d2"
});
const requestMarkdownEvent = core.workflowEvent({
    debugLabel: "markdown-request",
    uniqueId: "aa4c2e3c-0f09-4035-aab6-c72719c877cc"
});
const requestTextEvent = core.workflowEvent({
    debugLabel: "text-request",
    uniqueId: "6976536e-5399-4285-9455-0b70f1dfc92b"
});
const requestJsonEvent = core.workflowEvent({
    debugLabel: "json-request",
    uniqueId: "9fc4a330-52ad-4aac-8092-a650998b7f6f"
});
const markdownResultEvent = core.workflowEvent({
    debugLabel: "markdown-result",
    uniqueId: "2dfb57c8-73d1-4394-bea8-f05246d934d4"
});
const textResultEvent = core.workflowEvent({
    debugLabel: "text-result",
    uniqueId: "a27deec6-b24f-4eda-a5ac-ba2fb2bf37c8"
});
const jsonResultEvent = core.workflowEvent({
    debugLabel: "json-result",
    uniqueId: "e086e6bd-a612-4f25-ab41-9b31dcb8af86"
});

const URLS = {
    us: "https://api.cloud.llamaindex.ai",
    eu: "https://api.cloud.eu.llamaindex.ai",
    "us-staging": "https://api.staging.llamaindex.ai"
};
const { withState, getContext } = state.createStatefulMiddleware((params)=>{
    const apiKey = params.apiKey ?? env.getEnv("LLAMA_CLOUD_API_KEY");
    const region = params.region ?? "us";
    if (!apiKey) {
        throw new Error("LLAMA_CLOUD_API_KEY is not set");
    }
    return {
        cache: {},
        client: J(w({
            baseUrl: URLS[region],
            headers: {
                Authorization: `Bearer ${apiKey}`
            }
        }))
    };
});
const llamaParseWorkflow = withState(traceEvents.withTraceEvents(core.createWorkflow()));
llamaParseWorkflow.handle([
    uploadEvent
], async ({ data: form })=>{
    const { state } = getContext();
    const finalForm = {
        ...form
    };
    if ("file" in form) {
        // support loads from the file system
        const file = form?.file;
        const isFilePath = typeof file === "string";
        const data = isFilePath ? await env.fs.readFile(file) : file;
        const filename = isFilePath ? env.path.basename(file) : undefined;
        finalForm.file = data ? globalThis.File && filename ? new File([
            data
        ], filename) : new Blob([
            data
        ]) : undefined;
    }
    const { data: { id, status } } = await uploadFileApiV1ParsingUploadPost({
        throwOnError: true,
        body: {
            ...finalForm
        },
        client: state.client
    });
    state.cache[id] = status;
    return checkStatusEvent.with(id);
});
llamaParseWorkflow.handle([
    checkStatusEvent
], pRetry.pRetryHandler(async ({ data: uuid })=>{
    const { state } = getContext();
    if (state.cache[uuid] === "SUCCESS") {
        return checkStatusSuccessEvent.with(uuid);
    }
    const { data: { status } } = await getJobApiV1ParsingJobJobIdGet({
        throwOnError: true,
        path: {
            job_id: uuid
        },
        client: state.client
    });
    state.cache[uuid] = status;
    if (status === "SUCCESS") {
        return checkStatusSuccessEvent.with(uuid);
    }
    throw new Error(`LLamaParse status: ${status}`);
}, {
    retries: 100
}));
//#region sub workflow
llamaParseWorkflow.handle([
    requestMarkdownEvent
], async ({ data: job_id })=>{
    const { state } = getContext();
    const { data } = await getJobResultApiV1ParsingJobJobIdResultMarkdownGet({
        throwOnError: true,
        path: {
            job_id
        },
        client: state.client
    });
    return markdownResultEvent.with(data.markdown);
});
llamaParseWorkflow.handle([
    requestTextEvent
], async ({ data: job_id })=>{
    const { state } = getContext();
    const { data } = await getJobTextResultApiV1ParsingJobJobIdResultTextGet({
        throwOnError: true,
        path: {
            job_id
        },
        client: state.client
    });
    return textResultEvent.with(data.text);
});
llamaParseWorkflow.handle([
    requestJsonEvent
], async ({ data: job_id })=>{
    const { state } = getContext();
    const { data } = await getJobJsonResultApiV1ParsingJobJobIdResultJsonGet({
        throwOnError: true,
        path: {
            job_id
        },
        client: state.client
    });
    return jsonResultEvent.with(data.pages);
});
const upload = async (params)=>{
    //#region upload event
    const context = llamaParseWorkflow.createContext(params);
    const { stream, sendEvent } = context;
    const ev = uploadEvent.with(params);
    sendEvent(ev);
    const uploadThread = await llamaParseWorkflow.substream(ev, stream).until((ev)=>checkStatusSuccessEvent.include(ev)).toArray();
    //#region
    const jobId = uploadThread.at(-1).data;
    return {
        get signal () {
            // lazy load
            return context.signal;
        },
        get jobId () {
            return jobId;
        },
        get form () {
            return ev.data;
        },
        get context () {
            return context;
        },
        async markdown () {
            const requestEv = requestMarkdownEvent.with(jobId);
            const { sendEvent, stream } = llamaParseWorkflow.createContext(params);
            sendEvent(requestEv);
            const markdownThread = await stream.until(markdownResultEvent).toArray();
            return markdownThread.at(-1).data;
        },
        async text () {
            const requestEv = requestTextEvent.with(jobId);
            const { sendEvent, stream } = llamaParseWorkflow.createContext(params);
            sendEvent(requestEv);
            const textThread = await stream.until(textResultEvent).toArray();
            return textThread.at(-1).data;
        },
        //eslint-disable-next-line @typescript-eslint/no-explicit-any
        async json () {
            const requestEv = requestJsonEvent.with(jobId);
            const { sendEvent, stream } = llamaParseWorkflow.createContext(params);
            sendEvent(requestEv);
            const jsonThread = await stream.until((ev)=>jsonResultEvent.include(ev)).toArray();
            return jsonThread.at(-1).data;
        }
    };
};

exports.upload = upload;
